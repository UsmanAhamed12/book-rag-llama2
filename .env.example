# ============================================================
# Book RAG Assistant
# Example environment configuration
# ============================================================

APP_NAME="Book RAG Assistant"
APP_VERSION=1.0.0
ENVIRONMENT=development
LOG_LEVEL=INFO

# ------------------------------------------------------------
# Authentication
# Generate a secure value locally with:
# openssl rand -hex 32
# ------------------------------------------------------------

SECRET_KEY=replace_with_a_secure_random_secret
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ------------------------------------------------------------
# PostgreSQL
# Replace username/password/database as required.
# ------------------------------------------------------------

POSTGRES_URL=postgresql+psycopg://postgres:your_password@localhost:5432/book_rag

# ------------------------------------------------------------
# Ollama
# ------------------------------------------------------------

OLLAMA_MODEL=llama3.2
OLLAMA_HOST=http://localhost:11434
LLM_PROVIDER=ollama
BEDROCK_MODEL=apac.amazon.nova-lite-v1:0
BEDROCK_REGION=ap-south-1

# ------------------------------------------------------------
# Embeddings
# ------------------------------------------------------------

EMBEDDING_MODEL=BAAI/bge-small-en-v1.5

# ------------------------------------------------------------
# Retrieval / reranking
# Enable locally when the cross-encoder model is available.
# ------------------------------------------------------------

RERANKING_ENABLED=false
RERANKER_MODEL=cross-encoder/ms-marco-MiniLM-L6-v2
RETRIEVAL_CANDIDATE_K=10
RETRIEVAL_TOP_K=5
RETRIEVAL_MINIMUM_SCORE=0.35
RERANKER_WEIGHT=0.7

# ------------------------------------------------------------
# Storage
# ------------------------------------------------------------

CHROMA_DB_PATH=data/chroma
CHROMA_PATH=data/chroma
UPLOAD_DIR=data/uploads
